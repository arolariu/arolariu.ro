name: "website-official-build"

permissions:
  id-token: write
  contents: read
  issues: write
  pull-requests: write

defaults:
  run:
    working-directory: ./sites/arolariu.ro

on:
  push:
    branches: ["preview"]
    paths: ["sites/arolariu.ro/**"]
  workflow_dispatch:
    inputs:
      environment:
        description: "The environment to deploy to. (e.g. 'development', 'production')"
        options:
          - "DEVELOPMENT"
          - "PRODUCTION"
        default: "DEVELOPMENT"
        required: true
        type: choice
      container_name:
        description: "The name of the container to build and deploy. (e.g. 'frontend/dev.arolariu', 'frontend/arolariu')"
        options:
          - "frontend/dev.arolariu"
          - "frontend/arolariu"
        default: "frontend/dev.arolariu"
        required: true
        type: choice

env:
  SITE_NAME: ${{ inputs.environment == 'PRODUCTION' && 'arolariu.ro' || 'dev.arolariu.ro' }}
  SITE_URL: ${{ inputs.environment == 'PRODUCTION' && 'https://arolariu.ro' || 'https://dev.arolariu.ro' }}
  SITE_ENV: ${{ inputs.environment || 'development'}}
  API_NAME: "api.arolariu.ro"
  API_URL: "https://api.arolariu.ro"
  COMMIT_SHA: ${{ github.sha }}
  USE_CDN: ${{ inputs.environment == 'PRODUCTION' && 'true' || 'false' }}
  AZURE_CLIENT_ID: ${{secrets.AZURE_FRONTEND_MANAGED_IDENTITY_CLIENT_ID}}
  AZURE_TENANT_ID: ${{secrets.AZURE_TENANT_ID}}

jobs:
  test:
    name: 🧪 Test phase (1/2)
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'development'}}
    timeout-minutes: 10
    steps:
      - name: 🛫 Checking out the branch inside the runner environment...
        uses: actions/checkout@v4.2.2

      - name: 📦 Installing Node.js...
        uses: actions/setup-node@v4.4.0
        with:
          node-version: latest

      - name: 🚧 Adding environment variables...
        run: |
          touch .env
          echo "TIMESTAMP=$(date -u  +"%Y-%m-%dT%H:%M:%S"Z)" >> .env
          json='${{ toJson(env) }}' # We convert the env object to it's JSON representation.
          for key in $(echo $json | jq -r 'keys[]'); do
            value=$(echo $json | jq -r '.'$key)
            echo "$key=$value" >> .env
          done

      - name: 🚧 Adding environment secrets...
        run: |
          echo "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}" >> .env
          echo "CLERK_SECRET_KEY=${{ secrets.CLERK_SECRET_KEY }}" >> .env
          echo "API_JWT=${{ secrets.API_JWT }}" >> .env
          echo "RESEND_API_KEY=${{ secrets.RESEND_API_KEY }}" >> .env
          echo "CONFIG_STORE=${{ secrets.CONFIG_STORE }}" >> .env

      - name: 📦 Installing dependencies...
        run: |
          corepack enable
          yarn install --immutable
          yarn playwright install --with-deps
          yarn generate
        working-directory: ./sites/arolariu.ro

      - name: 🧪 Running Jest Unit tests...
        run: yarn test:jest
        working-directory: ./sites/arolariu.ro

      - name: 🧪 Running Playwright E2E Component tests...
        run: yarn test:playwright
        working-directory: ./sites/arolariu.ro
        env:
          CI: "true"

      - name: 🧪 Preparing UI tests (Building Storybook)...
        run: yarn storybook:build --quiet
        working-directory: ./sites/arolariu.ro

      - name: 🧪 Running Smoke & Integration tests (Playwright on Storybook)...
        run: |
          yarn dlx concurrently -k -s first -n "SB,TEST" -c "magenta,blue" \
            "yarn dlx http-server storybook-static --port 6006 --silent" \
            "yarn dlx wait-on tcp:6006 && yarn storybook:test"
        working-directory: ./sites/arolariu.ro

      - name: 📤 Upload Playwright Test Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ inputs.environment || 'development' }}-${{ github.run_id }}
          path: sites/arolariu.ro/playwright-report/
          retention-days: 7

      - name: ❓ Find Pull Request
        if: github.event_name == 'push' && github.ref == 'refs/heads/preview'
        id: find_pr
        uses: jwalton/gh-find-current-pr@v1
        with:
          state: open

      - name: 💬 Comment on Pull Request with Test Results
        if: always() && steps.find_pr.outputs.pr && github.event_name == 'push' && github.ref == 'refs/heads/preview'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = ${{ steps.find_pr.outputs.pr }};
            if (!prNumber || prNumber === 'null' || prNumber === '') {
              console.log('No open PR found for this commit on the preview branch, or find_pr step did not output a PR number.');
              return;
            }

            const jobStatus = '${{ job.status }}';
            const commitSha = '${{ github.sha }}'.substring(0, 7);
            let commentBody = `## Playwright Test Results for Workflow Run [#${{ github.run_id }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})\n\n`;
            commentBody += `**Branch:** \`${{ github.ref_name }}\`\n`;
            commentBody += `**Commit:** \`${commitSha}\`\n\n`;

            if (jobStatus === 'success') {
              commentBody += '✅ All Playwright tests passed! Screenshots for each test are included in the report.\n\n';
            } else if (jobStatus === 'failure') {
              commentBody += '❌ Playwright tests failed. Screenshots for each test are included in the report to help with debugging.\n\n';
            } else {
              commentBody += `⚠️ Playwright tests status: ${jobStatus}. Screenshots may be available in the report.\n\n`;
            }
            commentBody += `🔗 [View Full Test Report (including screenshots) Artifact](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts)\n`;

            try {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: parseInt(prNumber),
                body: commentBody
              });
              console.log(`Commented on PR #${prNumber}`);
            } catch (error) {
              console.error(`Failed to comment on PR #${prNumber}: ${error.message}`);
              if (error.status === 404) {
                console.log('PR not found, it might have been closed or merged.');
              } else if (error.status === 403) {
                console.log('Permission denied. Ensure GITHUB_TOKEN has issues:write or pull-requests:write permission.');
              }
            }

  build:
    name: ⚒️ Build phase (2/2)
    needs: test
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'development'}}
    steps:
      - name: 🛫 Checking out the branch inside the runner environment...
        uses: actions/checkout@v4.2.2

      - name: 🔒 Performing auth against Azure Public Cloud...
        uses: Azure/login@v2.3.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: 🔒 Performing auth against private Azure Container Registry...
        uses: Azure/docker-login@v2
        with:
          username: ${{ secrets.AZURE_CONTAINER_REGISTRY_USERNAME }}
          password: ${{ secrets.AZURE_CONTAINER_REGISTRY_PASSWORD }}
          login-server: ${{ secrets.AZURE_CONTAINER_REGISTRY_ADDRESS }}

      - name: 🚧 Adding environment variables...
        run: |
          touch .env
          echo "TIMESTAMP=$(date -u  +"%Y-%m-%dT%H:%M:%S"Z)" >> .env
          json='${{ toJson(env) }}'
          for key in $(echo $json | jq -r 'keys[]'); do
            value=$(echo $json | jq -r '.'$key)
            echo "$key=$value" >> .env
          done

      - name: 🚧 Adding environment secrets...
        run: |
          echo "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}" >> .env
          echo "CLERK_SECRET_KEY=${{ secrets.CLERK_SECRET_KEY }}" >> .env
          echo "API_JWT=${{ secrets.API_JWT }}" >> .env
          echo "RESEND_API_KEY=${{ secrets.RESEND_API_KEY }}" >> .env
          echo "CONFIG_STORE=${{ secrets.CONFIG_STORE }}" >> .env

      - name: 🏗️ Building the docker container...
        env:
          CONTAINER_NAME: ${{inputs.container_name || 'frontend/dev.arolariu'}}
          CONTAINER_TAG: ${{inputs.environment == 'PRODUCTION' && github.sha || 'latest'}}
        run: docker build -t ${{ secrets.AZURE_CONTAINER_REGISTRY_ADDRESS }}/${{env.CONTAINER_NAME}}:${{env.CONTAINER_TAG}} .

      - name: ⏫ Pushing the docker container...
        env:
          CONTAINER_NAME: ${{inputs.container_name || 'frontend/dev.arolariu'}}
          CONTAINER_TAG: ${{inputs.environment == 'PRODUCTION' && github.sha || 'latest'}}
        run: docker push ${{ secrets.AZURE_CONTAINER_REGISTRY_ADDRESS }}/${{env.CONTAINER_NAME}}:${{env.CONTAINER_TAG}}
