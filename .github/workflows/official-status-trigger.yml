name: "official-status-trigger"

permissions:
  contents: read

on:
  push:
    branches: ["main"]
    paths:
      - "sites/status.arolariu.ro/**"
      - ".github/actions/setup-workspace/**"
      - ".github/actions/static-web-app-build-deploy/**"
      - ".github/workflows/official-status-trigger.yml"
      - ".github/workflows/reusable-static-webapp.yml"

  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment for deployment."
        options:
          - "production"
        default: "production"
        type: choice

jobs:
  deploy:
    name: 🚀 Status static web app
    if: github.repository == 'arolariu/arolariu.ro'
    uses: ./.github/workflows/reusable-static-webapp.yml
    with:
      site-name: "status"
      working-directory: "sites/status.arolariu.ro"
      test-command: "npm run test:unit"
      build-command: "npm run prepare && npm run build"
      app-location: "./sites/status.arolariu.ro/"
      api-location: "./sites/status.arolariu.ro/build/server"
      output-location: "build/static"
      environment-name: "status"
      site-url: "https://status.arolariu.ro"
    secrets:
      SWA_TOKEN: ${{ secrets.AZURE_STATIC_WEB_APPS_STATUS_TOKEN }}
