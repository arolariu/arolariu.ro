{
  "name": "@arolariu/status",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview --port 4175",
    "prepare": "svelte-kit sync || echo ''",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test": "npm run test:unit",
    "test:unit": "svelte-kit sync && vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "svelte-kit sync && vitest run --coverage",
    "test:e2e": "playwright test",
    "probe": "node scripts/probe.ts",
    "probe:all": "node scripts/probe.ts && node scripts/aggregate.ts && node scripts/detectIncidents.ts"
  },
  "devDependencies": {
    "@playwright/test": "*",
    "@sveltejs/kit": "*",
    "@sveltejs/vite-plugin-svelte": "*",
    "@testing-library/jest-dom": "*",
    "@testing-library/svelte": "*",
    "@types/node": "*",
    "jsdom": "*",
    "sass": "*",
    "svelte-adapter-azure-swa": "*",
    "svelte-check": "*",
    "svelte": "*",
    "typescript": "*",
    "vite": "*",
    "vitest": "*"
  }
}
