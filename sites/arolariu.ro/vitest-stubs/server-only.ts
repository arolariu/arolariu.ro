/**
 * @fileoverview Stub for server-only package in Vitest tests.
 * @module vitest-stubs/server-only
 *
 * @remarks
 * The `server-only` package prevents code from being bundled in client-side code.
 * In tests, we don't need this restriction, so we provide an empty stub.
 */

// Empty stub - does nothing
export {};
