/** @format */

import type {AvailableLanguageTag} from "@/i18n/runtime.js"; //generated file
import {createI18n} from "@inlang/paraglide-next";

/* eslint-disable */

// All available functions exported.
export const {middleware, Link, useRouter, usePathname, redirect, permanentRedirect, localizePath} =
  createI18n<AvailableLanguageTag>();

/* eslint-enable */
